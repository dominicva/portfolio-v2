import { Overpass_Mono, Overpass } from 'next/font/google';

export const overPassMono = Overpass_Mono({
  subsets: ['latin'],
  display: 'swap',
});

export const overPass = Overpass({
  subsets: ['latin'],
  display: 'swap',
});
